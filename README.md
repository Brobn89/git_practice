# newProject
Git Commands Cheat Sheet
$git init
$git clone
$git status
$ git add
$ git diff [file]
$ git diff --staged [file]
$ git checkout -- [file]
$ git reset [file]
$ git commit -m “[descriptive message]”
